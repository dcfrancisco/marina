// Array operations
LOCAL arr, i

arr := {10, 20, 30, 40, 50}

? "Array:", arr
? "First element:", arr[0]
? "Third element:", arr[2]

// Loop through array
FOR i := 0 TO 4
    ? "Element", i, ":", arr[i]
NEXT
